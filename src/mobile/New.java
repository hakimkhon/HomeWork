package mobile;

public class New {
    public static void main(String[] args) {
        System.out.println(("xsadaxxa"));

    }
    public static int countX(String str){

        return 1;
    }

    public static String removeLastChar(String s) {
        return (s == null || s.length() == 0)
                ? null
                : (s.substring(0, s.length() - 1));
    }
}
